    <div>
        {{#each responseFromAPI}}
            <img src="{{this.image_url}}" alt="{{this.name}}">
            <h3>{{this.name}}</h3>
            <p>{{this.description}}</p>
            <p><small>{{this.brewers_tips}}</small></p>
        {{/each}}
    </div>